'use strict'

class QlWith {
  register (Model, options) {
    // const defaultOptions = {}
    // const options = Object.assign(defaultOptions, customOptions)

    Model.queryMacro('addWith', function (value) {
      
     
     
      
    })
  }
}

module.exports = QlWith
